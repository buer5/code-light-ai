{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the tray application",
  "windows": ["background"],
  "permissions": [
    "core:default",
    "opener:default",
    "autostart:default"
  ]
}
